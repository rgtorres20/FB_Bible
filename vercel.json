{
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {
        "// includeFiles": "Without this, only the entrypoint is bundled and `import app` fails at invocation with FUNCTION_INVOCATION_FAILED.",
        "includeFiles": "app/**"
      }
    }
  ],
  "// routes": "Must be `routes`, not `rewrites`: when a `builds` key is present Vercel uses legacy routing and silently ignores `rewrites`, 404ing every path.",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}
